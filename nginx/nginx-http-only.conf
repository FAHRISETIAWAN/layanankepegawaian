server {
    listen 80;
    server_name itms.atrbpn.go.id;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 200 'Nginx OK - menunggu SSL cert';
        add_header Content-Type text/plain;
    }
}
